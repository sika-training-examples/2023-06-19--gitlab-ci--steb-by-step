global_before:
  - curl -fsSL https://raw.githubusercontent.com/ondrejsika/training-cli/master/install.sh | sh

stages:
  - foo
  - bar

foo:
  stage: foo
  script: training-cli version

bar:
  stage: bar
  script: training-cli --help