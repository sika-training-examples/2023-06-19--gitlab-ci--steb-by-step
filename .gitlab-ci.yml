Build Foo:
  script: cd foo && echo docker build .... && cat Dockerfile
  rules:
    - changes:
        - foo/**

Build Bar:
  script: cd foo && echo docker build .... && cat Dockerfile
  rules:
    - changes:
        - bar/**
